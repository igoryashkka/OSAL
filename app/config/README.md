# App Configuration

This folder contains application-level configuration files.

## Files

- `app_config.h` - Application configuration constants (task names, intervals, priorities)
- Other configuration headers as needed

Configuration values can be adjusted here without modifying the core application logic.
