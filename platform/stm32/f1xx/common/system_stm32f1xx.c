// /* Minimal CMSIS system file just to satisfy HAL. For production use the official ST version. */
// #include "stm32f1xx.h"

// /* Default system clock after reset is HSI (8 MHz). */
// uint32_t SystemCoreClock = 8000000U;

// void SystemInit(void)
// {
//     /* Keep default reset clocking. Vector table is at 0x08000000 by default. */
//     /* If you relocate VECTORTABLE, configure SCB->VTOR here. */
// }

// void SystemCoreClockUpdate(void)
// {
//     /* For simplicity keep it constant; HAL will update after SystemClock_Config. */
// }
